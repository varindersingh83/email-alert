[pytest]
testpaths = tests
addopts = -ra -p no:cacheprovider
pythonpath = .
markers =
    integration: requires a dedicated PostgreSQL test database
    telegram_e2e: sends a real message through the configured Telegram bot
